version: '3'

services:
  redis:
    image: 'redis:5'
    restart: on-failure
    environment:
      - REDIS_HOST=${REDIS_HOST:-127.0.0.1}
      - REDIS_PASSWORD=${REDIS_PASSWORD:-null}
      - REDIS_PORT=${REDIS_PORT:-6379}
    volumes:
      - ./storage/redis:/var/www/storage/redis

  client:
    build:
      context: ./
      dockerfile: services/client/Dockerfile
    volumes:
      - ./:/var/www
      - /var/www/node_modules
    environment:
      - APP_ENV=${APP_ENV:-production}
    command: ./services/client/init.sh

  api:
    restart: on-failure
    build:
      context: ./
      dockerfile: services/api/Dockerfile
    volumes:
      - ./:/var/www
      - /var/www/vendor
      - ./services/api/default.ini:/usr/local/etc/php/conf.d/default.ini
      - ./services/api/supervisord.conf:/etc/supervisor/conf.d/supervisord.conf
      - ./services/api/scheduler.conf:/etc/cron.d/scheduler.conf
    depends_on:
      - redis
    command: ./services/api/init.sh

  nginx:
    restart: on-failure
    image: 'nginx:latest'
    volumes:
      - ./:/var/www
      - ./services/nginx/default.conf:/etc/nginx/conf.d/default.conf
    ports:
      - ${NGINX_PORT:-8000}:80
    depends_on:
      - client
      - api
